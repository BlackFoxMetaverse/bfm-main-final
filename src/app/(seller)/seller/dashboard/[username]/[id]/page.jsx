import SellerPortfolio from '@/components/seller/layout/seller_dashboard/sellerProfile'
import React from 'react'

const page = ({params}) => {
  return (
    <SellerPortfolio params={params} />
  )
}

export default page
